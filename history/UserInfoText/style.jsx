import styled from 'styled-components';
import { px } from '@/styles';

export default {
  UserInfoText: styled.div`
    color: black;
    white-space: nowrap;
    text-align: center;
    font-weight: 300;
    font-size: ${px(20)};
    margin-top: ${px(10)};
  `,
};
