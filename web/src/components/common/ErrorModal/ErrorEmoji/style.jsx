import styled from 'styled-components';
import { px } from '@/styles/themeUtil';

export default {
  ErrorEmoji: styled.div.attrs({ type: 'div' })`
    font-size: ${px(180)};
    text-align: center;
  `,
};
