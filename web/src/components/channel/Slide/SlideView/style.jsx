import styled from 'styled-components';
import { px } from '@/styles/themeUtil';

export default {
  Wrapper: styled.div`
    flex: 1;
    width: 94%;
    position: relative;
    padding: ${px(20)} 0;
  `,
};
