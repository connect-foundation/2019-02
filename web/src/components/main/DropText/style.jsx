import styled from 'styled-components';
import { px } from '@/styles';

export default {
  DropText: styled.div`
    color: white;
    white-space: nowrap;
    text-align: center;
    font-weight: 300;
    font-size: ${px(76)};
  `,
};
