name: CD
on:
  pull_request:
    types: [closed]
    branches:
      - master
jobs:
  deploy:
    name: Deploy All Application
    runs-on: ubuntu-latest
    if: github.event.pull_request.merged == true
    steps:
      - run: |
        echo "Preparing auto deployment"
      